#include <stdio.h>

/**
 * main - print the number of argument passed into the program
 * @argc: int
 * @argv: list
 * Return: 0
*/

int main(int argc, char const *argv[])
{
	int l = 0;

	while (argc--)
	{
		printf("%s\n", argv[i]);
		l++;
	}
	return (0);
}
